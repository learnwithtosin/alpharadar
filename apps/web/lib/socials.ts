/**
 * Real. The GitHub repo already exists at this URL.
 */
export const GITHUB_URL = "https://github.com/learnwithtosin/alpharadar";

/**
 * PLACEHOLDER — no X/Twitter account exists yet. `null` on purpose: the
 * Footer renders GitHub as a real link but X as a disabled, non-clickable
 * label whenever this is null, so a dead link never ships by accident.
 * Set this to the real profile URL once the account exists — the footer
 * switches to a live link automatically, no other change needed.
 */
export const X_URL: string | null = null;
